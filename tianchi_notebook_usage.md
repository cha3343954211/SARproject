# 阿里天池平台 SAR图像目标检测Notebook使用说明

## 1. 概述

本Notebook专为阿里天池平台设计，实现了完整的SAR图像多类别有向目标检测流程，包括数据准备、模型训练、评估和提交结果生成。本说明将指导您如何在阿里天池平台上有效地使用该Notebook。

## 2. 平台环境准备

### 2.1 选择合适的GPU环境

在阿里天池平台上运行本Notebook时，建议选择以下环境配置：
- 计算资源：选择至少1个GPU的实例（推荐V100或A100）
- 内存：推荐16GB以上
- 存储：至少需要10GB可用空间用于存储数据和模型

### 2.2 上传Notebook和数据

1. 在阿里天池平台创建一个新的Notebook任务
2. 上传`tianchi_sar_detector_notebook.ipynb`文件
3. 确保您已下载并解压比赛数据集

## 3. 数据准备

### 3.1 数据集结构

在运行Notebook前，请确保您的数据集按照以下结构组织：

```
SAR_data/
├── train/
│   ├── images/  # 训练图像文件（.png格式）
│   └── annfiles/  # 训练标注文件（.txt格式，DOTA格式）
└── test_A/
    ├── images/  # 测试图像文件（.png格式）
    └── annfiles/  # 测试标注文件（可选）
```

### 3.2 修改数据路径

在Notebook的第2节"数据集准备"中，根据您的实际数据位置修改以下路径：

```python
# 设置数据目录
DATA_DIR = '/home/tianchi/SAR_data'  # 改为您的数据集路径
TRAIN_DATA_DIR = os.path.join(DATA_DIR, 'train')
TEST_DATA_DIR = os.path.join(DATA_DIR, 'test_A')
PROCESSED_DIR = os.path.join(DATA_DIR, 'processed')
```

## 4. 运行Notebook

### 4.1 按顺序执行单元格

请按照Notebook中的顺序依次执行每个单元格，建议不要跳过任何步骤：

1. **环境配置**：安装必要的依赖包
2. **数据集准备**：预处理数据并划分训练集和验证集
3. **数据集可视化**：查看部分数据样本
4. **创建配置文件**：生成模型配置
5. **模型训练**：启动训练过程
6. **模型评估**：在验证集上评估模型性能
7. **模型推理**：可视化检测结果
8. **生成提交结果**：创建比赛提交文件
9. **模型调优建议**：获取进一步提升性能的建议

### 4.2 关键参数调整

根据您的需求和硬件条件，可以调整以下关键参数：

- 在配置文件创建部分，您可以修改`model`部分的参数来调整模型结构
- 在数据准备部分，您可以修改`val_ratio`来调整训练集和验证集的划分比例
- 在模型训练部分，您可以调整学习率、批量大小等超参数

## 5. 常见问题解决

### 5.1 内存不足问题

如果遇到内存不足的错误，可以尝试：
- 减小批量大小（`samples_per_gpu`）
- 使用更小的图像尺寸
- 减少数据增强的复杂度

### 5.2 GPU资源限制

如果GPU显存不足，可以尝试：
- 使用轻量级的骨干网络（如将ResNet50改为ResNet34）
- 减小特征通道数
- 使用梯度累积

### 5.3 训练中断后恢复

如果训练过程中断，可以通过以下方式恢复：
```python
!python tools/train.py ../configs/sar_detector_tianchi.py --work-dir ../work_dirs/sar_detector --resume-from ../work_dirs/sar_detector/latest.pth
```

## 6. 结果提交

训练完成后，Notebook会自动生成`submit_result.zip`文件，包含所有测试图像的检测结果。您可以直接将此文件上传至阿里天池比赛平台进行提交。

## 7. 模型调优指南

如需进一步提高模型性能，可以参考Notebook中第9节的调优建议，包括：
- 尝试不同的骨干网络
- 增强数据增强策略
- 调整优化器和学习率
- 模型集成
- 处理类别不平衡问题

## 8. 注意事项

1. 请确保您的网络连接稳定，尤其是在下载依赖包和训练模型时
2. 训练过程可能需要较长时间，建议在GPU实例上运行
3. 定期保存检查点，以便在训练中断时恢复
4. 如有任何问题，请参考MMRotate官方文档或在阿里天池平台寻求帮助

祝您取得好成绩！